apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: kubecart-image-updater
  namespace: argocd
spec:
  applicationRefs:
    - namePattern: kubecart-dev
      useAnnotations: true
    - namePattern: kubecart-prod
      useAnnotations: true
