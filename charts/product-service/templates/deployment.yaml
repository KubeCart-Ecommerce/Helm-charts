{{- $svc := index .Values.global "product-service" -}}
{{- $imageValues := index .Values "product-service" | default dict -}}
{{- $image := index $imageValues "image" | default dict -}}
{{- $imageRepository := index $image "repository" | default $svc.image.repository -}}
{{- $imageTag := index $image "tag" | default $svc.image.tag -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: product-service
spec:
  replicas: {{ $svc.replicaCount }}
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: "{{ $imageRepository }}:{{ $imageTag }}"
          imagePullPolicy: {{ $svc.imagePullPolicy }}
          ports:
            - containerPort: {{ $svc.containerPort }}
          envFrom:
            - configMapRef:
                name: product-service-config
            - secretRef:
                name: product-service-secret
          readinessProbe:
{{- toYaml $svc.readinessProbe | nindent 12 }}
          livenessProbe:
{{- toYaml $svc.livenessProbe | nindent 12 }}
          resources:
{{- toYaml $svc.resources | nindent 12 }}
