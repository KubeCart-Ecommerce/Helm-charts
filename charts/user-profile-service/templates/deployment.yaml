{{- $svc := index .Values.global "user-profile-service" -}}
{{- $imageValues := index .Values "user-profile-service" | default dict -}}
{{- $image := index $imageValues "image" | default dict -}}
{{- $imageRepository := index $image "repository" | default $svc.image.repository -}}
{{- $imageTag := index $image "tag" | default $svc.image.tag -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-profile-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: user-profile-service
spec:
  replicas: {{ $svc.replicaCount }}
  selector:
    matchLabels:
      app: user-profile-service
  template:
    metadata:
      labels:
        app: user-profile-service
    spec:
      containers:
        - name: user-profile-service
          image: "{{ $imageRepository }}:{{ $imageTag }}"
          imagePullPolicy: {{ $svc.imagePullPolicy }}
          ports:
            - containerPort: {{ $svc.containerPort }}
          envFrom:
            - configMapRef:
                name: user-profile-service-config
            - secretRef:
                name: user-profile-service-secret
          readinessProbe:
{{- toYaml $svc.readinessProbe | nindent 12 }}
          livenessProbe:
{{- toYaml $svc.livenessProbe | nindent 12 }}
          resources:
{{- toYaml $svc.resources | nindent 12 }}
